[package]
name = "provider-challenge"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
repository.workspace = true
description = "Challenge responder service: polls on-chain challenges and answers them with storage proofs"

[dependencies]
async-trait = { workspace = true }
provider-chain = { workspace = true }
sp-core = { workspace = true, features = ["std"] }
sp-runtime = { workspace = true, features = ["std"] }
storage-primitives = { workspace = true, features = ["std"] }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true, features = ["std"] }
