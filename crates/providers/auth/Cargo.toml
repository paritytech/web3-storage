[package]
name = "provider-auth"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
repository.workspace = true
description = "Provider HTTP authentication: signed Authorization header building/verification, bucket membership resolution and role enforcement"

[dependencies]
async-trait = { workspace = true }
dashmap = { workspace = true }
hex = { workspace = true, features = ["std"] }
sp-core = { workspace = true, features = ["std"] }
storage-primitives = { workspace = true, features = ["std"] }
storage-subxt = { workspace = true }
subxt = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true, features = ["std"] }
