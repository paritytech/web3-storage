[settings]
timeout = 1000
provider = "native"

[relaychain]
default_command = "polkadot"
chain = "rococo-local"

  [[relaychain.nodes]]
  name = "alice"
  validator = true
  rpc_port = 9900

  [[relaychain.nodes]]
  name = "bob"
  validator = true
  rpc_port = 9901

[[parachains]]
id = 4000
chain_spec_path = "/Users/naren/DevBox/scalable-web3-storage/chain-specs/storage-local.json"

  [parachains.collator]
  name = "storage-collator"
  command = "polkadot-omni-node"
  rpc_port = 9944
