{
  "name": "@web3-storage/core",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-free, browser-safe primitives: byte/hex utils, HTTP fetch with retry, base64, provider request signing, CID verification. No chain coupling, no descriptors.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@polkadot-labs/hdkd-helpers": "^0.0.30"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.9"
  }
}
